name: Preview

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  preview:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write # Allow comments on PRs
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: npm

      - name: 🏗 Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Publish to Expo
        run: npx expo publish --release-channel=pr-${{ github.event.number }} --non-interactive

      - name: 💬 Comment in preview
        uses: expo/expo-github-action/preview-comment@v8
        with:
          channel: pr-${{ github.event.number }}

      # - name: 💬 Comment PR
      #   uses: thollander/actions-comment-pull-request@v2
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     comment_tag: execution
      #     message: |
      #       🚀 Expo preview is ready!
      #       - Project → **${{ steps.preview.outputs.projectName }}**
      #       - Runtime Version → **${{ steps.preview.outputs.runtimeVersion }}**
      #       - Project QR: `${{ steps.preview.outputs.projectQR }}`            
      #       - **[More info](${{ steps.preview.outputs.projectLink }})**

      #       > Learn more about [𝝠 Expo Github Action](https://github.com/expo/expo-github-action/tree/main/preview#example-workflows)

      #       <details>
      #       <summary>Additional Information</summary>

      #       - Project Owner: `${{ steps.preview.outputs.projectOwner }}`
      #       - Project Slug: `${{ steps.preview.outputs.projectSlug }}`
      #       - Project Deep Link: `${{ steps.preview.outputs.projectDeepLink }}`
      #       </details>
